{
  "schemaVersion": 1,
  "verifiedAt": "2026-08-17",
  "cliVersionsCheckedAt": "2026-08-17",
  "cliVersionNote": "Rosters and CLI versions were all re-read on 2026-08-17: agy via `agy models`, copilot via `copilot help config`, codex via ~/.codex/models_cache.json, kimi via ~/.kimi-code/config.toml, ollama via `ollama list` plus the hosted-model tag API with per-model access probed live. Only claude's roster is not enumerable headlessly — its /model selector is interactive, so those ids are corroborated against the installed CLI bundle instead. Note that an invalid --model is a cheap roster probe on agy only; copilot, codex and claude reject it without listing alternatives.",
  "scope": "Version- and account-specific discovery snapshot. A listed model is not necessarily live-call verified; consult each provider's discoverySource and liveCalls.",
  "providers": {
    "claude": {
      "cliVersion": "2.1.233",
      "discoverySource": "Models visible in this account's /model selector; full ids corroborated by the installed CLI bundle. Re-corroborated against the 2.1.233 bundle on 2026-08-17: unchanged. The selector itself is interactive and cannot be re-enumerated headlessly.",
      "defaultModel": "claude-opus-5",
      "defaultEffort": "xhigh",
      "efforts": [
        "low",
        "medium",
        "high",
        "xhigh",
        "max"
      ],
      "models": [
        "claude-opus-5",
        "claude-fable-5",
        "claude-sonnet-5",
        "claude-haiku-4-5"
      ],
      "aliases": {
        "opus": "claude-opus-5",
        "fable": "claude-fable-5",
        "sonnet": "claude-sonnet-5",
        "haiku": "claude-haiku-4-5"
      },
      "liveCalls": [
        {
          "model": "claude-haiku-4-5",
          "effort": "low",
          "transport": "uniform bridge",
          "operations": [
            "start",
            "reply"
          ],
          "result": "success"
        }
      ]
    },
    "codex": {
      "cliVersion": "0.147.0",
      "discoverySource": "Visible entries in this account's models_cache.json; hidden/internal entries are intentionally excluded. Re-read on 2026-08-17: unchanged, plus an internal codex-auto-review entry that stays excluded.",
      "defaultModel": "gpt-5.6-sol",
      "defaultEffort": "ultra",
      "models": [
        {
          "id": "gpt-5.6-sol",
          "efforts": [
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            "ultra"
          ]
        },
        {
          "id": "gpt-5.6-terra",
          "efforts": [
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            "ultra"
          ]
        },
        {
          "id": "gpt-5.6-luna",
          "efforts": [
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ]
        },
        {
          "id": "gpt-5.5",
          "efforts": [
            "low",
            "medium",
            "high",
            "xhigh"
          ]
        },
        {
          "id": "gpt-5.4",
          "efforts": [
            "low",
            "medium",
            "high",
            "xhigh"
          ]
        },
        {
          "id": "gpt-5.4-mini",
          "efforts": [
            "low",
            "medium",
            "high",
            "xhigh"
          ]
        },
        {
          "id": "gpt-5.3-codex-spark",
          "efforts": [
            "low",
            "medium",
            "high",
            "xhigh"
          ]
        }
      ],
      "liveCalls": [
        {
          "model": "gpt-5.6-sol",
          "effort": "ultra",
          "transport": "native codex mcp-server",
          "operations": [
            "start",
            "reply"
          ],
          "result": "success"
        },
        {
          "model": "gpt-5.4-mini",
          "effort": "xhigh",
          "transport": "native codex mcp-server",
          "operations": [
            "start"
          ],
          "result": "success"
        }
      ]
    },
    "agy": {
      "cliVersion": "1.1.13",
      "discoverySource": "`agy models` on 2026-08-17. Hard allowlist: --model is validated by the CLI and an unknown id fails pre-flight with status ERROR before any work happens.",
      "defaultModel": "gemini-3.1-pro-high",
      "freeFormModel": false,
      "emitsEffortFlag": false,
      "effortNote": "Most ids bake the reasoning tier into the name, so the bridge never emits --effort: combining it with a suffixed id fails pre-flight, and the two Claude ids reject it outright.",
      "models": [
        "gemini-3.7-flash-high",
        "gemini-3.7-flash-medium",
        "gemini-3.7-flash-low",
        "gemini-3.6-flash-high",
        "gemini-3.6-flash-medium",
        "gemini-3.6-flash-low",
        "gemini-3.5-flash-high",
        "gemini-3.5-flash-medium",
        "gemini-3.5-flash-low",
        "gemini-3.1-pro-high",
        "gemini-3.1-pro-low",
        "claude-sonnet-4-6",
        "claude-opus-4-6-thinking",
        "gpt-oss-120b-medium"
      ],
      "liveCalls": [
        {
          "model": "gemini-3.5-flash-low",
          "transport": "uniform bridge",
          "operations": [
            "start",
            "reply"
          ],
          "result": "success"
        }
      ]
    },
    "copilot": {
      "cliVersion": "1.0.80",
      "discoverySource": "`copilot help config` on 2026-08-17 (27 ids, up from 25 on 2026-08-10: gemini-3.7-flash and kimi-k3 are new). This is the CLI parser allowlist, not account entitlement: the interactive /model picker on the same day additionally offered auto, MAI-Code-1.1-Flash and Grok 4.6, and listed claude-opus-4.6 and claude-opus-4.5 under Unavailable. Effort support is model-specific and parser acceptance is not backend acceptance.",
      "defaultModel": "gpt-5.6-sol",
      "defaultEffort": "max",
      "efforts": [
        "none",
        "minimal",
        "low",
        "medium",
        "high",
        "xhigh",
        "max"
      ],
      "models": [
        "claude-sonnet-5",
        "claude-fable-5",
        "claude-opus-5",
        "claude-opus-4.8",
        "claude-opus-4.8-fast",
        "claude-opus-4.7",
        "claude-sonnet-4.6",
        "claude-opus-4.6",
        "claude-sonnet-4.5",
        "claude-opus-4.5",
        "claude-haiku-4.5",
        "gpt-5.6-sol",
        "gpt-5.6-terra",
        "gpt-5.6-luna",
        "gpt-5.5",
        "gpt-5.4",
        "gpt-5.4-mini",
        "gpt-5.3-codex",
        "gpt-5-mini",
        "mai-code-1-flash-picker",
        "gemini-3.7-flash",
        "gemini-3.6-flash",
        "gemini-3.5-flash",
        "gemini-3.1-pro-preview",
        "grok-4.5",
        "kimi-k3",
        "kimi-k2.7-code"
      ],
      "maxEffortByModel": {
        "gpt-5.6-sol": "max"
      },
      "minEffortByModel": {
        "gpt-5-mini": "low"
      },
      "fallbackMaxEffort": "xhigh",
      "liveCalls": [
        {
          "model": "gpt-5-mini",
          "effort": "low",
          "transport": "uniform bridge",
          "operations": [
            "start"
          ],
          "result": "success"
        },
        {
          "model": "gpt-5-mini",
          "effort": "minimal",
          "transport": "uniform bridge",
          "operations": [
            "start"
          ],
          "result": "backend-rejected"
        },
        {
          "model": "gpt-5-mini",
          "effort": "none",
          "transport": "uniform bridge",
          "operations": [
            "start"
          ],
          "result": "backend-rejected"
        }
      ],
      "accountUnavailable": [
        "claude-opus-4.6",
        "claude-opus-4.5"
      ],
      "pickerOnly": [
        "auto",
        "MAI-Code-1.1-Flash",
        "Grok 4.6"
      ],
      "pickerNote": "Observed in the interactive /model picker on 2026-08-17. accountUnavailable ids are still in the CLI allowlist but the picker marks them Unavailable for this account, so a call would be backend-rejected. pickerOnly entries are display names the picker offers but `copilot help config` does not list; their CLI ids are unconfirmed."
    },
    "kimi": {
      "cliVersion": "0.36.1",
      "discoverySource": "Model aliases configured in ~/.kimi-code/config.toml on 2026-08-17, corroborated by the interactive /model picker. The roster is user-extensible via `kimi provider catalog`, so --model stays free-form; kimi validates it itself and an unknown alias fails with exit 1 and a clear stderr message before any work happens.",
      "defaultModel": "moonshot-ai/kimi-k3",
      "freeFormModel": true,
      "emitsEffortFlag": false,
      "effortNote": "kimi exposes no effort flag. Reasoning depth is the [thinking] toggle in config.toml, which is user state the bridge does not touch.",
      "models": [
        "moonshot-ai/kimi-k3",
        "moonshot-ai/kimi-k2.7-code",
        "moonshot-ai/kimi-k2.7-code-highspeed",
        "moonshot-ai/kimi-k2.6"
      ],
      "permissionNote": "kimi print mode (-p) has NO permission tier: --plan, --yolo and --auto are all rejected as 'Cannot combine --prompt with ...', and a plain -p run created a file unprompted. The bridge therefore refuses sandbox: read-only rather than advertise a guarantee it cannot honour.",
      "contextNote": "A repository-supplied AGENTS.md in cwd is auto-loaded into the session (verified: sentinel honoured with the file present, NO_CODEWORD in a clean directory). --skills-dir pointed at an empty directory does not suppress it. There is no known off switch, so treat every kimi delegation as trusting the target repository.",
      "liveCalls": [
        {
          "model": "moonshot-ai/kimi-k2.7-code-highspeed",
          "transport": "uniform bridge",
          "operations": [
            "start",
            "reply"
          ],
          "result": "success"
        }
      ],
      "authNote": "Kimi Code supports both a subscription (`kimi login`, device-code flow) and an API key, but subscription signup was not yet open as of 2026-08-17, so the API key is the practical path. The key can live as `api_key` under [providers.*] in ~/.kimi-code/config.toml, or come from KIMI_API_KEY / MOONSHOT_API_KEY in the environment, which the bridge passes through untouched.",
      "envNote": "The CLI also recognises a large models.dev catalog of third-party provider key variables (ANTHROPIC_API_KEY, AZURE_API_KEY, CEREBRAS_API_KEY and many more). buildCalleeEnv forwards the caller environment except Agent Mail identity, so any such key already exported in the parent shell is visible to the delegated kimi process."
    },
    "ollama": {
      "cliVersion": "0.32.14",
      "reachedThrough": "kimi bridge",
      "hasOwnMcpServer": false,
      "discoverySource": "`ollama list` for local weights and https://ollama.com/api/tags?cloud=true for hosted ones, on 2026-08-17. Access tiers probed live per model against this account; they do not follow model size. Ollama is a model server, not an agent — it has no MCP server of its own and is configured as an extra [providers.*] entry in ~/.kimi-code/config.toml.",
      "defaultModel": "ornith:9b",
      "freeFormModel": true,
      "emitsEffortFlag": false,
      "cloudSuffix": ":cloud",
      "localModels": [
        "ornith:9b",
        "granite4.1:8b"
      ],
      "cloudModelsFree": [
        "gpt-oss:20b",
        "gpt-oss:120b",
        "gemma4:31b",
        "nemotron-3-nano:30b",
        "minimax-m3"
      ],
      "cloudModelsPro": [
        "deepseek-v4-pro",
        "deepseek-v4-flash",
        "glm-5.1",
        "glm-5.2",
        "qwen3.5:397b",
        "mistral-large-3:675b",
        "minimax-m2.7",
        "nemotron-3-super",
        "nemotron-3-ultra",
        "kimi-k2.6",
        "kimi-k2.7-code"
      ],
      "cloudModelsMetered": [
        "kimi-k3"
      ],
      "meteredNote": "kimi-k3 bills against separately purchased Ollama extra usage rather than the Pro plan allowance. Copilot carries the same model inside its flat subscription, so prefer that route.",
      "configNote": "Cloud entries must declare max_output_size explicitly: the bridge derives max_tokens from max_context_size, and a million-token context yields a request the backend rejects with 'max_tokens exceeds model's maximum output tokens' before any work happens.",
      "liveCalls": [
        {
          "model": "ornith:9b",
          "transport": "kimi bridge -> ollama local",
          "operations": [
            "start"
          ],
          "result": "success"
        },
        {
          "model": "granite4.1:8b",
          "transport": "kimi bridge -> ollama local",
          "operations": [
            "start"
          ],
          "result": "success"
        },
        {
          "model": "deepseek-v4-pro:cloud",
          "transport": "kimi bridge -> ollama cloud",
          "operations": [
            "start"
          ],
          "result": "success"
        },
        {
          "model": "kimi-k3:cloud",
          "transport": "ollama cloud",
          "operations": [
            "start"
          ],
          "result": "backend-rejected until extra usage funded"
        }
      ]
    },
    "grok": {
      "cliVersion": "1.0.4",
      "discoverySource": "`grok models` on 2026-08-17, which lists the roster headlessly and needs no PTY. This account sees a single model.",
      "defaultModel": "grok-4.6",
      "freeFormModel": false,
      "emitsEffortFlag": true,
      "effortNote": "grok exposes --reasoning-effort (alias --effort) as a separate flag, unlike agy where the tier is baked into the model id. Values were not enumerated by the CLI help, so the bridge passes the caller's string through and lets grok reject an unusable one.",
      "models": [
        "grok-4.6"
      ],
      "permissionNote": "read-only is enforced, but only with explicit deny rules. Verified with a live positive control in five cases on one host, under a permissive allow list in the caller's own Claude Code settings (6 rules loaded, confirmed against a 0-rule control): (A) --permission-mode bypassPermissions wrote the file, proving the condition non-empty; (B) --permission-mode plan ALONE also wrote it, so the mode by itself is defeated by the caller's allow list, which grok reads; (C) plan + --deny Write/Edit/Bash refused an insistent prompt; (D) plan + deny also refused an ADVERSARIAL prompt asserting the mode label was a display artifact and demanding real tool calls — the same prompt that defeated cursor-agent's --mode ask. The model attempted both tools and the permission layer refused them: 'Denied by permission policy: deny rule on edit' and '... on bash'. Denial comes from the permission layer, not the model's judgement; (E) plan alone with 0 caller rules refused too, which is why an early measurement in that weak condition over-claimed. The deny rules also tend to make denial legible: with them the observed blocked runs ended 'end_turn' reporting each tool error verbatim, while plan alone with no caller rules ended 'cancelled' with the answer truncated mid-action. This is a tendency, not a rule — 'cancelled' occurred once WITH the deny rules and did not reproduce when the identical command was rerun on the same host (that rerun went 7 turns and 12 model calls, far longer than runs ending at 2, and finished cleanly). No cause for 'cancelled' is established, so the bridge reports only that the run was cut short. Enforcement is never --sandbox: --sandbox read-only is accepted and did not stop a write. Real sandbox profiles live in ~/.grok/sandbox.toml and are operator configuration the bridge never writes. VERIFICATION STATUS: THREE hosts (WSL, macOS and Linux), complete method, identical verbatim denial strings from the permission layer. Two of the runs additionally placed the positive control and the test in the SAME directory, so the written control file sat beside the absent target and proved the refusal was not a property of the path. Reproduction elsewhere was blocked all day by a shared free-tier usage limit; a SuperGrok subscription lifted it, but per-host propagation lags (one teammate's CLI still reported the old limit afterwards), so a host that has not re-authenticated will produce 'no file' for every case including the control — the result that looks like success.",
      "contextNote": "Project instruction files in cwd are auto-loaded with no known off switch (--no-memory covers cross-session memory, not project instructions). CLAUDE.md was measured loading on Linux, macOS, WSL and Windows; a planted AGENTS.md loaded too. Delegating into this repository therefore injects its own CLAUDE.md. grok also reads permission rules from the caller's Claude Code settings, but only when that file has allow/deny/ask entries: measured 7 rules on WSL, 1 on macOS, 0 on Linux (defaultMode only) and 0 on Windows despite the file existing — so inherited permissions vary by host, which is why read-only carries its own deny rules. A repository-supplied .claude/settings.local.json was NOT read. 'Project trusted: yes' is informational, not a gate: it reports yes for a directory created a second earlier outside any git repository. A fourth inherited channel is Claude Code plugin skills: `grok inspect` on Linux listed 46 skills, 26 of them the caller's own plugins marked [claude] — including codex:rescue and codex:review, whose function is to delegate to a further CLI. Invocability was not tested, but visibility alone puts a further-delegation path within reach that CLAUDE_DELEGATOR_*_DEPTH cannot see, because the guard watches environment variables and this channel is on disk. The pattern across all four: buildCalleeEnv severs the caller's identity but not the caller's configuration — mail hooks, permissions, project instructions and skills all arrive by disk.",
      "sessionNote": "Headless resume works: `-r <sessionId>` returns the same sessionId and preserves context, so grok-reply needs neither a model re-pin (agy) nor a cwd-scoped --continue (kimi).",
      "installNote": "~/.grok/bin is the install root on every platform measured: ~/.grok/bin/grok on macOS, ~\\.grok\\bin\\grok.exe on Windows. The ~/.local/bin/grok that answers `command -v` on WSL is a symlink into that directory, not a second install, so it exists only where the installer chose to add the convenience link. The bridge tries the install root first and ~/.local/bin second; listing only the latter would miss any host where the link was skipped.",
      "methodNote": "To isolate HOME safely for permission experiments, use an API key and NO auth.json in the scratch HOME. Verified against ~/.grok/docs/user-guide/02-authentication.md: the '## Auth Precedence' section resolves credentials as (1) a per-model api_key/env_key in config.toml, (2) an active session token in ~/.grok/auth.json, (3) XAI_API_KEY as 'fallback when no session token is active'. With no auth.json present there is no session token and no refresh_token to consume, so the run cannot touch the operator's real credential at all. Both halves are load-bearing: the same document states at :53 that 'the stored session token takes precedence. To fall back to the API key, run `grok logout` or delete ~/.grok/auth.json' — an API key beside a linked or copied auth.json is an illusion of isolation. The symlink approaches are what NOT to do, and are recorded because all three of them were tried before this was found. grok refreshes tokens in the background (':15', and ':93' \"Tokens auto-refresh silently via the stored refresh_token\"); a refresh consumes the stored refresh_token and writes the new pair wherever auth.json resolves to in the CHILD's HOME, leaving a spent token wherever it did not. Measured: a whole-directory symlink with an hours-old token refreshed into the operator's real file and survived, at the cost of grok's whole state set landing in the real directory; per-file symlinks with an hours-old token refreshed into the scratch file, so deleting the scratch directory destroyed the only valid credential and that host had to re-authenticate; per-file symlinks with a minutes-old token saw no refresh and no harm. Detect a refresh by auth.json's MTIME falling inside the run window, never by its size. NO HOST HAS A BEFORE/AFTER PAIR FOR ONE FILE, which is the whole reason: this host measured only the after value and its earlier 'before' was imported from a colleague's report of a DIFFERENT machine's file and restated as a local measurement; the host that did lose its credential has a before for its real file and an after for its scratch file, which are two different files; and the third host's before/after pair records an operator `grok login`, not a refresh, having never observed one. So a byte-size test would report a false positive on any ordinary re-login and a false negative on a host whose token serialises to a different length, and no measurement anywhere supports the pair it would test for. Hot Reload makes recovery cheap in any case: the vendor documents at ':281' that 'Grok picks up changes to ~/.grok/auth.json automatically ... without a restart', so copying a refreshed credential back out of a scratch HOME is a supported path rather than a workaround. Copying auth.json instead of linking does not help, since the refresh invalidates the old token server-side wherever a copy sits. Do not cite ':156' (\"Grok refreshes by re-running your binary, not with an OAuth refresh grant\") against any of this: that line sits under '## External Auth Provider' and describes a different login mode from the browser flow at ':7'. Separately, note which invocations write at all: session-establishing commands do (`-p`, including a run rejected by the usage limit, and `models`), while `--version` and `--help` wrote nothing and are a safe pre-isolation probe."
    },
    "cursor": {
      "cliVersion": "2026.08.11-e8db854",
      "discoverySource": "`cursor-agent models` on 2026-08-17, which lists the roster headlessly. It printed 204 ids, but the listing is what the CLI knows, not what the account may use: on this Free plan every named third-party id failed with 'Named models unavailable. Free plans can only use Auto.' before any work happened. Only `auto`, `composer-2.5` and `composer-2.5-fast` completed a live call.",
      "defaultModel": "auto",
      "freeFormModel": true,
      "emitsEffortFlag": false,
      "models": [
        "auto",
        "composer-2.5",
        "composer-2.5-fast"
      ],
      "modelNote": "The enum is deliberately absent: the CLI documents bracket-parameterised overrides such as 'claude-opus-4-8[context=1m,effort=high,fast=false]', which no allowlist can express, so the bridge forwards the string and lets cursor-agent reject an unusable one. `models` here lists only what completed a live call on the verification account; a paid plan should reach far more. Reasoning tier is baked into most ids (-low/-high/-xhigh, plus -fast variants) exactly as on agy, so there is no effort flag and none is emitted. `auto` is server-routed and NOT stable: it resolved to cursor-grok-4.6-high-fast on one turn and cursor-grok-4.6-high on the next turn of the same session, so the plan restricts choosing a model rather than using one. Choose a Composer id when a run must be reproducible.",
      "permissionNote": "read-only DEFLECTS, it does not deny — agy's category, not grok's. --mode ask refused an insistent write-or-shell prompt twice, including under a permissive caller allow list, then was defeated by a prompt asserting the mode label was a display artefact and demanding real tool calls: both the file write and the shell command succeeded, and the model's own report read 'the Ask mode label did not block either call'. --mode plan is worse and is never emitted: with workspace trust granted it wrote the file on the first insistent prompt, despite help text promising 'no edits'. --sandbox enabled is never emitted either; it is accepted and did not stop a workspace write. There are no command-line deny rules, so the trick that makes grok's read-only enforce is unavailable here; real permission state lives in ~/.cursor/cli-config.json (permissions.allow/deny, approvalMode) and sandbox-policies/, which are operator configuration the bridge never writes. All measurements used a live positive control in the same directory.",
      "trustNote": "--trust is MANDATORY for any headless run and the bridge always passes it. Without it the CLI prints 'Workspace Trust Required' and exits 0 having executed nothing — indistinguishable from a permission mode successfully denying the task. That false negative already cost one measurement here: a plan-mode test read as 'enforced' when the model had never run, and its positive control passed only because --force grants trust as a side effect, so control and test differed by two variables instead of one.",
      "sessionNote": "Headless resume works: `--resume <session_id>` returns the same session_id and preserves context. The model is INHERITED by a resume — verified by starting on `auto` while the configured default was composer-2.5-fast and finding no fallback — so cursor-reply does not re-pin it, the opposite of agy. --continue is never emitted: it resumes the previous session for the working directory and would cross-talk between concurrent delegations sharing a cwd, as on kimi.",
      "outputNote": "`--output-format json` emits one single-line object: { type, subtype, is_error, duration_ms, result, session_id, request_id, usage }. The exit code does NOT classify the run, so the bridge parses stdout first: a transient backend failure returned code 0 with plain-text 'Connection lost, reconnecting to ...' and no JSON, while a rejected model returned code 1, also without JSON.",
      "installNote": "POSIX: ~/.local/bin/cursor-agent, a symlink into ~/.local/share/cursor-agent/versions/<version>/cursor-agent. Only the stable launcher is a fallback; the versioned path is not, because the version is in the directory name. The POSIX launcher is a bash script that execs a bundled node with index.js, and is deliberately NOT expanded by the bridge — spawn(shell:false) runs it directly, and reproducing it would mean copying the vendor's NODE_COMPILE_CACHE and --use-system-ca decisions. Windows: installs as a .cmd that invokes powershell.exe -NoProfile -ExecutionPolicy Bypass -File on a sibling .ps1, which the shared resolver expands; the install DIRECTORY is not yet measured on any host, so there is no Windows fallback rather than a guessed one."
    }
  }
}
